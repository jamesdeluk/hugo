# Copyright Notice
copyright: © 2024 Copyright.

# A disclaimer notice for the footer. Make sure you have set "params.footer.disclaimer.enable: true" in your `config.yaml` file.
disclaimer: "Me"

# Meta description for your site.  This will help the search engines to find your site.
description: "Portfolio and personal website of James Gibbins: Data science, business and consulting, climate and energy, IT and cyber security, and more."

# specify the list of custom menus that you want to show in the top navbar.
# they will be separated by a divider from the main menus.
customMenus:
- name: Tags
  url: /tags
  # specify whether to hide the menu item from top navbar or not.
  hideFromNavbar: false
  # specify whether to show the menu item in footer or not.
  # if you set it to true, make sure you have set `footer.navigation.customMenus: true` in your config.yaml
  showOnFooter: true

- name: Search
  url: /search
  # specify whether to hide the menu item from top navbar or not.
  hideFromNavbar: false
  # specify whether to show the menu item in footer or not.
  # if you set it to true, make sure you have set `footer.navigation.customMenus: true` in your config.yaml
  showOnFooter: true

# Specify OpenGraph Headers
openGraph:
  title: James's Peredutions
  type: website
  description: Portfolio and personal website of James Gibbins
  image: images/site/hshysrot.gif
  url: https://www.jamesgibbins.com
